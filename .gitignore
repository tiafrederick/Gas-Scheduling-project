# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Local canonical store (rebuildable from data/samples + data/raw; not source of truth)
*.duckdb
*.duckdb.wal
data/warehouse/

# data/raw/ IS tracked: it's the small, provenance-stamped landing zone (fetched
# EBB files + .meta.json sidecars) that backs claims in design-decision-log.md.
# If it ever grows into bulk/regenerable dumps, gitignore it then — not preemptively.

# OS / editor
.DS_Store
.idea/
.vscode/
